# Continuous Integration Workflow
# Runs tests and linting on push to main and pull requests
#
# Copy this file to .github/workflows/ci.yml in your repository

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint --if-present

  test:
    name: Test (Node ${{ matrix.node-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18, 20, 22]
      fail-fast: false

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Upload coverage report
        if: matrix.node-version == '20'
        uses: codecov/codecov-action@v4
        with:
          fail_ci_if_error: false
        continue-on-error: true

  # Optional: Integration tests with Docker
  # Uncomment if you have integration tests that require Docker
  #
  # integration:
  #   name: Integration Tests
  #   runs-on: ubuntu-latest
  #   needs: [lint, test]
  #   services:
  #     node-red:
  #       image: nodered/node-red:latest
  #       ports:
  #         - 1880:1880
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #
  #     - name: Install dependencies
  #       run: npm ci
  #
  #     - name: Wait for Node-RED
  #       run: |
  #         timeout 60 bash -c 'until curl -s http://localhost:1880/ > /dev/null; do sleep 2; done'
  #
  #     - name: Run integration tests
  #       run: npm run test:integration
